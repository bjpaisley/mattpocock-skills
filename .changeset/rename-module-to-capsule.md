---
"mattpocock-skills": minor
---

Rename the `codebase-design` glossary term **module** to **capsule** across the skill set (glossary, `improve-codebase-architecture` report rules, consumer skills, docs pages, READMEs, doc-authoring guide). "Module" is overloaded in this fork's target codebases (a Python import unit, a Verilog design unit in RTL automation), and joins the term's `_Avoid_` list. Quotations, `CHANGELOG.md`, JS `module` keywords, and the `setup-ts-deep-modules` skill name are untouched. Rationale in `.agents/adr/0003-rename-module-to-capsule.md`.
